{
  "name": "rit-now-server",
  "version": "0.0.1",
  "description": "Counts the number of users connected to a single page app.",
  "dependencies": {
    "express": "^4.10.2",
    "socket.io": "^1.3.2"
  }
}
